# Copy to .env and fill in (never commit .env).
# RevenueCat dashboard → Project → API keys → Secret API key (starts with sk_)

REVENUECAT_SECRET_API_KEY=sk_MwUMpFBVqySIeXECtQropFEGvSNzZ

# Optional: entitlement id in RevenueCat (default in code is "premium")
# REVENUECAT_ENTITLEMENT_ID=premium

# Set to 0 for local testing without verifying IAP on signup
# SIGNUP_REQUIRES_PAYMENT=0
